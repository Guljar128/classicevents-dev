export class Stats{
    number:string;
    label:string;
    icon:string;
    color:string;       
    constructor(number:string, label:string, icon:string, color:string){
        this.number=number;
        this.label=label;
        this.icon=icon;
        this.color=color;
    }
}